# Phase 5 uses local JSON content by default.
DATA_SOURCE=local

# Future provider option. With output="export", this API is read at build time.
# DATA_SOURCE=api
# CONTENT_API_URL=https://api.example.com
